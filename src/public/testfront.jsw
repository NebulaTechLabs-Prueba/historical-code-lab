/**
 * Este archivo `.jsw` sirve como un módulo backend en Wix Velo, permitiendo exponer funciones personalizadas que pueden ser llamadas de forma segura desde el frontend.
 * Su razón de existir es encapsular la lógica del servidor, proteger datos sensibles y facilitar la comunicación entre el cliente y el backend, asegurando así una arquitectura más segura y mantenible.
 */